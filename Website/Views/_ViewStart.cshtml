@using Website.Common.Viewmodels
@model ViewModel

@{
    if (Model.Overlay)
    {
        Layout = "_OverlayLayout";
    }
    else if (Model.JsPage)
    {
        Layout = "_AjaxLayout";
    } else
    {
        Layout = "_Layout";
    }
}