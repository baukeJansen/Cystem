@using Website.Common.Viewmodels
@using Website.Common.Models.EAV
@using Website.Common.Enums
@model GenericViewModel

<li class="collection-item param">
    <div class="row">
        <div class="col s10 l11">Param</div>
        <div class="col s2 l1">
            @await Render.EditButton("/cystem/edit-value/", new { id = Model.Value.Id }, "", "edit", Model.Options, ElementAction.Overlay, new { @class = "right" })
        </div>

        <div class="col s10 l11 right">
            @if (Model.Value.RelatedValue != null)
            {
                <div class="row">
                    <ul class="collection value-collection">
                        @await Render.Value(Model.Value.RelatedValue, Model.Options)
                    </ul>
                </div>
            }
        </div>
    </div>

    @{ await Cystem.DisplayChilds(Model.Value, DisplaySetting.Alter); }
</li>