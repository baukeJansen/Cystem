@using Website.Common.Viewmodels
@model GenericViewModel

<li class="collection-item group">
    <div class="row">
        <div class="col s12">@Model.Value.Group.Label</div>
    </div>

    @{ await Cystem.DisplayChilds(Model.Value); }
</li>