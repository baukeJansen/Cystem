@using Website.Common.Enums
@using Website.Common.Viewmodels
@model GenericViewModel

<div class="container">
    <div class="row">
        <div class="col s12">
            <h1>@Html.DisplayValue(Model, "Title")</h1>
        </div>
    </div>

    <div class="row">
        <div class="col s12">
            <a href="@Url.Action("create-page", "cystem")" class="ajax-get right btn-floating waves-effect waves-light btn-large" data-on-result="@ElementAction.Overlay"><i class='material-icons'>add</i></a>
        </div>
    </div>

    <div class="row">
        @Html.DisplayValue(Model, "Pages")
    </div>
</div>
