@using Website.Common.Viewmodels
@model GenericViewModel

<div class="container">
    @using (Html.BeginForm("edit-page", "cystem", new { }, FormMethod.Post, true, new { }))
    {
        @*Html.HiddenFor(m => m.Where(v => v.Attribute.Label == "id"))
        *@
        <div class="row">
            <div class="col s12">
                <h2>@Html.DisplayValue(Model, "Title")</h2>
            </div>
        </div>

        @Html.EditValue(Model, "Name");
        @Html.EditValue(Model, "Url");

        /*if (Model != null)
        {
            foreach (Value value in Model)
            {
                @Html.EditValue(Model, "Name");
            }
        }*/
    }
</div>