@using Website.Common.Models.EAV
@model List<Value>

<div class="container">
    <div class="row">
        <div class="col s12">
            <h1>@Html.DisplayValue(Model, "Title")</h1>

            <p>
                @Html.DisplayValue(Model, "Text")
            </p>
        </div>
    </div>
</div>