@using Website.Common.Viewmodels
@model BundleViewModel

@{
    IEnumerable<Task> tasks = Model.Results.Select(r => Html.RenderPartialAsync(r.ViewName, r.Model));
    await Task.WhenAll(tasks);

    Layout = "";
}

@*await Component.InvokeAsync("CystemCommonHeader", Model)
@await Component.InvokeAsync("CystemCommonFooter", Model)*@